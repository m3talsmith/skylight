{
  "name": "skylight",
  "version": "0.0.0",
  "description": "A demo toggling a light switch on a page over skynet",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^0.9.16",
    "socket.io-client": "^0.9.16",
    "skynet": "0.0.33",
    "express": "^3.5.1"
  }
}
